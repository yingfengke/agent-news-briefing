# AI & Agent 开发者晨报 — Python 依赖
# 安装: pip install -r requirements.txt

# API 密钥管理
python-dotenv

# 动态网页爬虫（Playwright）
playwright

# 内容指纹去重（MinHash + LSH）
datasketch

# 中文分词（MinHash 预处理）
jieba

# 向量计算
numpy>=1.24.0

# RSS/Atom 解析（比 xml.etree 更鲁棒，支持松散格式 + content:encoded）
feedparser

# 语义去重聚类
scikit-learn>=1.3.0
