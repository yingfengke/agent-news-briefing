# Python 依赖
# 安装: pip install -r requirements.txt
# 锁定日期: 2026-07-04
# 更新: pip freeze | grep -iE 'python-dotenv|datasketch|jieba|numpy|feedparser|scikit-learn|sgmllib3k|tiktoken|beautifulsoup4|jinja2' > requirements.txt

# API 密钥管理
python-dotenv==1.2.2

# 内容指纹去重（MinHash + LSH）
datasketch==1.10.0

# 中文分词（MinHash 预处理）
jieba==0.42.1

# 向量计算
numpy==2.4.6

# RSS/Atom 解析（比 xml.etree 更鲁棒，支持松散格式 + content:encoded）
feedparser==6.0.12

# feedparser 依赖
sgmllib3k==1.0.0

# 语义去重聚类
scikit-learn==1.9.0

# Token 精确估算
tiktoken==0.9.0

# HTML 解析（GitHub Trending 抓取）
beautifulsoup4==4.15.0

# 邮件模板引擎
jinja2==3.1.6
